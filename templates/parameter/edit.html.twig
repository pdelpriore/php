{% extends 'base.html.twig' %}
{% block title %}{{ titleConfiguration }}{% endblock %}

{% block body %}
    <div class="container">
        <div class="col-md-12 text-center">
            <h2>{{ parameter.paramKey }}</h2>
        </div>

        <div class="form-group">
            {{ include('parameter/_form.html.twig') }}

            <div class="row col-md-2">
                <a href="{{ path('parameter_index') }}" class="btn btn-block btn-sm btn-default">Retour</a>
            </div>
            <div class="col-md-2">
                {{ include('parameter/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}