{% extends 'base.html.twig' %}
{% block title %}{{ titleConfiguration }}{% endblock %}

{% block body %}
    <div class="container">
    <div class="col-md-12 text-center">
        <h2>{{ billing.name }}</h2>
    </div>

    <div class="form-group">
        {{ include('billing/_form.html.twig') }}

            <div class="row col-md-2">
                <a href="{{ path('billing_index') }}" class="btn btn-block btn-sm btn-default">Retour</a>
            </div>
            <div class="col-md-2">
                {{ include('billing/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}