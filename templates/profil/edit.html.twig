{% extends 'base.html.twig' %}
{% block title %}{{ titleConfiguration }}{% endblock %}

{% block body %}
    <div class="container">
    <div class="col-md-12 text-center">
        <h2>{{ profil.name }}</h2>
    </div>

    <div class="col-md-12 form-group">
        {{ include('profil/_form.html.twig') }}

            <div class="col-md-2">
                <a href="{{ path('profil_index') }}" class="btn btn-block btn-sm btn-default">Retour</a>
            </div>
            <div class="col-md-2">
                {{ include('profil/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}