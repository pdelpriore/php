{#<form method="post" action="{{ path('activity_delete', {'id': activity.id}) }}" onsubmit="return confirm('Merci de confirmer la suppression de l\'activité {{ activity.name }}.');">#}
    {#<input type="hidden" name="_method" value="DELETE">#}
    {#<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ activity.id) }}">#}
    {#<button class="btn btn-block btn-sm btn-danger">Supprimer</button>#}
{#</form>#}